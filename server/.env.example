PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database>
JWT_SECRET=replace_with_long_random_secret

# Make.com webhook settings required for forgot-password OTP emails
MAKE_WEBHOOK_URL=https://hook.make.com/your_webhook_id
# Optional: API key sent in header `x-make-apikey`
MAKE_WEBHOOK_API_KEY=replace_with_shared_secret
